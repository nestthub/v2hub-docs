[project]
name = "v2hub-docs"
version = "0.0.1"
description = "Official documentation for the V2Hub developer ecosystem."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "zensical>=0.0.30",
]
